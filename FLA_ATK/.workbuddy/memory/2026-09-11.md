# 2026-09-11

- 用户要求"提交代码"：工作区干净，无新改动可提交；本地 main 领先远程，改为执行推送。
- 推送遇到默认代理 52470 失效（502）、直连超时；扫描本机代理端口发现 7897 可用，用它成功 push（7ee5382..c446f2b main -> main）。
- 推送成功后 origin/main 跟踪引用未更新（PortableGit packed-refs 问题），误报 ahead 11；手工改 packed-refs 修正，现 status 显示 main 与 origin/main 同步。
- 环境结论已写入 MEMORY.md（可用代理 7897、git 目录在父目录、ssh 246 注意事项）。

## 上午 11:08 拉取代码 + 配置 PyCharm
- git pull 同步到 e712396（Update temp_note）；发现本地还有未推送提交 fd6b6d2（Use ATK attr_tuple for ACL int arrays，用户自己提交的），已用代理 7897 推送，本地与远程同步。
- PortableGit packed-refs 问题再次出现，同法修复。
- PyCharm 配置（PyCharm CE 2024.3.1，未运行状态写入）：
  - 解释器：注册 Python 3.13.14（C:\Users\admin\.workbuddy\binaries\python\versions\3.13.12\python.exe）到 jdk.table.xml，名称 "Python 3.13 (workbuddy)"，关联 D:\t30072652\Project。
  - .idea 新增/更新：Project.iml、modules.xml、misc.xml、encodings.xml、vcs.xml（Git 映射）、runConfigurations/FLA_ATK_generators.xml（三个 gen_*.py 运行配置）。
  - 所有 XML 校验通过，6 个 py 文件 py_compile 通过。
  - 注意：项目 py 文件 import atk.*（华为 ATK 用例生成框架），本机未安装，导入检查会有 unresolved reference，属预期。
  - .idea/.gitignore 已忽略 workspace.xml 和 shelf/。

## 上午 11:22 push 失败排查
- 根因：用户/PyCharm 直接 push 走的是失效代理 127.0.0.1:52470（env 变量），报 502；7897 一直正常。
- 修复：把 http.proxy/https.proxy = http://127.0.0.1:7897 写入仓库级 git config（D:\t30072652\Project\.git\config），此后普通 git push/fetch/PyCharm 内置 git 均可直接用。
- 新提交 57358e3（Always generate causal conv query start tuple）已推送，本地与远程同步；packed-refs 跟踪引用这次 fetch 后自动更新正常了。

## 上午 11:29 generator 清理 range_values="null"
- 用户要求：去掉 generator 中 required=False 时把 range_values 设为 "null" 的逻辑。
- 修改 gen_recurrent_kda.py（4 处：ssmStateIndicesOptional、numAcceptedTokensOptional、aLogOptional、dtBiasOptional，改为仅在使用时才赋 range_values）和 gen_prepare_wy_repr_bwd_da.py（1 处：else 分支删掉 "null" 赋值行）。
- gen_causal_conv1d_bwd.py 本来就没有该逻辑，未动。
- executor 侧 _optional*/_optional_input 助手同时兼容 None 与 "null"，不受影响。
- 中途误回退过一次，用户纠正后重新执行，两文件 py_compile 通过。

## 上午 11:41 进一步注释掉 required=False 赋值
- 用户追加要求：required=False 的赋值逻辑也直接去掉/注释。
- 三个 generator 均改为：只在需要时设 required=True + range_values，False/None 分支用注释保留原逻辑（可选参数沿用 YAML 的 required: false 默认值）。
  - gen_causal_conv1d_bwd.py：yOptional 改为 activation!=0 时设 True。
  - gen_prepare_wy_repr_bwd_da.py：else 分支整体注释。
  - gen_recurrent_kda.py：ssmStateIndicesOptional/numAcceptedTokensOptional/aLogOptional/dtBiasOptional 的 False 赋值注释掉。
- 行为不变（YAML 里可选参数本来就是 required: false），三文件 py_compile 通过。

## 上午 11:45 generator range_values 改为从 yaml 取
- 用户新原则：除特殊计算逻辑外，generator 的 range_values 不硬编码，从 yaml（case_config 种子）取。
- 三个 generator 增加 _yaml_value(config, default) 助手（解析 yaml 值、容忍单元素列表、null 回退默认）。
- gen_causal_conv1d_bwd.py：activation 从 yaml 取（合法域 0/1/2 校验保留）；layout 轮换、queryStartLoc=[0,time] 属特殊逻辑保留硬编码；yOptional 仅 activation!=0 时设 required=True。
- gen_prepare_wy_repr_bwd_da.py：chunkSize 从 yaml 取（回退 profile 值）；cuSeqlens 从 yaml 取并归一化为 [首,尾]；chunkIndices 仍按 offsets×chunkSize 计算（特殊逻辑）；删掉原 `cfg["chunkSize"].range_values = bt` 覆盖。
- gen_recurrent_kda.py：cuSeqlens offsets 从 yaml 取（要求长度 b+1、首 0 尾 token_count，否则回退均匀网格）；ssmStateIndices 保留特殊计算；aLog/dtBias 不再硬编码范围，沿用 yaml；scale/lowerBound 不再覆盖，沿用 yaml；profile 布尔 attrs 仍显式赋值（驱动用例多样性）。
- 三文件 py_compile 通过，未提交。

## 下午 12:32 causal_conv1d_bwd 进一步泛化
- 资料：源仓 D:\生态仓测试\flash-linear-attention-npu\tests\atk\causal_conv1d_bwd（spec 驱动，仅 BSND/activation=0/无状态）；算子文档 README（支持 5 layout、activation 0/1/2、[B,W,D] 状态、TND/NTD 变长 B+1 段）。
- gen_causal_conv1d_bwd.py：新增 _segment_bounds（B+1 严格递增边界、余数按 index 轮转）；TND/NTD 用例段数按 index 轮转 2~6，queryStartLoc tuple_numbers 同步设为 [len(qsl)]；initialState/dht 形状恒定 [B,W,D]（varlen B=段数），并按 index%3!=0 轮转设 required=True 以驱动 dh0 输出路径。
- causal_conv1d_bwd.yaml：queryStartLocOptional 的 tuple_numbers 由 [2] 泛化为 [2,3,4,5,6,7]（generator 会重算实际长度）。
- executor_causal_conv1d_bwd.py：_query_start_loc_for_layout 守卫从"仅 len==2"放开为任意长度（首 0、尾 totalTokens、严格递增即透传），否则回退 [0,T]；使多段变长真正到达 CPU 标杆（按段循环）与 aclnn 路径（aclCreateIntArray）。
- 风险备注：attr_tuple 的 tuple_numbers 每用例可变长度未经真实 ATK 框架验证（本机无 atk），generator 用 hasattr 防御；需在服务器跑一轮确认。
- 新增 _sanity_check.py（离线桩验证 60 个索引的不变量，含 executor 守卫对拍），py_compile 与 sanity 全部通过，未提交。

## 下午 12:40 recurrent_kda / prepare_wy_repr_bwd_da 同法泛化
- 资料：源仓 tests/atk 两算子 gen 均为极窄 profile（recurrent_kda 仅 1 个 BSND profile；prepare_wy 仅 2 个固定 shape）；工作区 README 确认文档能力域。
- recurrent_kda/gen：新增 _segment_lengths（每段 1~8、余数按 index 轮转）；BSND 沿用 README batch 均分语义，TND 非索引用例轮转 1~4 段变长多序列（索引用例保持单段避免共享 state slot）；state pool 容量改为 ≥ 段数（TND 多段时 seq_count）；ssm 模式改 index%3 互质周期（0 无/1 packed/2 speculative），speculative 时 2 维 [seq_num,max_step] + numAcceptedTokens（(len+1)//2）联动 required；dtBias 在 useGate 一半用例上 required，扁平 [H_v*K]/矩阵 [H_v,K] 交替。layout 轮转保持 index%2（与 %3 互质，修了两次周期耦合 bug）。
- recurrent_kda.yaml：cuSeqlensOptional dim_values [2]→[2,3,4,5]。
- prepare_wy/gen：新增 _segment_units/_varlen_offsets（段长为 chunkSize 整数倍，README 5.2 语义）；TIMES 扩为 (64,128,256)；varlen 改为 index%4!=3（3/4 变长多段、1/4 定长 B∈{1,2,4} 覆盖 README 5.1）；yaml 2 元组种子只在 len>=3 且 chunk 对齐时才采纳（避免 [0,128] 固定种子压制多段泛化）；chunkIndices tuple_numbers 防御式同步为实际长度，删除 len==4 硬校验。
- prepare_wy.yaml：cuSeqlens tuple_numbers [2]→[2,3,4,5]、chunkIndices [4]→[2,4,6,8]。
- 两算子 executor 无需改动（golden 本就按段循环/支持 2 维 indices/双 dtBias 形状）。
- 各新增 _sanity_check.py：recurrent 200 用例全过（2 layout × 3 ssm 模式 × dtBias 全组合），prepare_wy 200 全过（varlen T{64,128,256} + fixed B{1,2,4}）；三 gen py_compile 通过，未提交。

## 下午 13:00 方向纠正：不动 FLA_ATK 既有目录，源仓泛化归档到新目录
- 用户澄清：**不是修改 FLA_ATK 下已有三个算子目录**（那里是用户自己 aclnn 风格的工作），而是以源仓 D:\生态仓测试\flash-linear-attention-npu\tests\atk\ 的 spec-driven 用例为底本做补充泛化，归档到 FLA_ATK 下的**新目录**。
- 已把上午/中午对 FLA_ATK 三个既有目录的全部改动还原到用户状态（git diff 验证仅剩用户自己的 3 个 gen 修改 + executor CRLF 差异），删除我加的 _sanity_check.py 与 __pycache__。
- 精读源仓确认能力域：causal_conv1d_bwd aclnn 支持 BSND/BSH/BNSD（定长）+TND/NTD（varlen, query_start_loc）、activation 0/1/2、initial_state/dht [B,W,D]；recurrent_kda 段长≤8、K=128 V∈{128,256}、packed [T]/speculative [seq,max_step]+numAcceptedTokens、dtBias flat/matrix、仅 BF16 q/k/v；prepare_wy_repr_bwd_da GVA（HV%HK==0）、V∈{128,256}、varlen 需 B=1 且 cu_seqlens/chunk_indices 传 python list（_aclnn_ctypes.py 证实）。
- 新建三个归档目录（yaml+gen+executor+README，spec-driven 风格与源仓一致，部署时拷入源仓 tests/atk/<op>/ 覆盖同名文件即可）：
  - FLA_ATK/causal_conv1d_bwd_gen/：8880 profiles = B/T/D/W 网格 × 2 dtype × 3 activation × 5 layout，with_state 奇偶交替，varlen 段长≥W（B*W≤T 才生成），query_start_loc 写入 case_spec；executor 按 layout reshape/permute，varlen 逐段反传叠加 dw/db，有 dht 时返回 4 元组（含 dh0）。
  - FLA_ATK/recurrent_kda_gen/：96 profiles = 2 layout × 3 ssm 模式 × 2 V × 2 dtBias × 4 (HV,H)；BSND 用满 B*T 容量（cu[-1]=B*T）、TND packed=总段长；speculative max_step≥max 段长；state_capacity 索引模式>段数；executor 打开 ssm_indices/numAccepted/dtBias/A_log 真实通路，标杆与源仓逐行一致（use_gate_in_kernel=True 闭环）。
  - FLA_ATK/prepare_wy_repr_bwd_da_gen/：30 profiles = 3 T × 5 GVA 对 × 2 V，varlen/fixed 交替、bf16/fp16 交替；varlen 段长为 chunk_size 倍数、chunk_indices 扁平 [seq,chunk,...]（重放校验一致）；executor 把 cu_seqlens/chunk_indices 真实传 NPU，CPU golden 完整移植 test_da.py::compute_dA_cpu（varlen get_bos_eos 切分）。
- 离线校验两套（managed python 装了 numpy+torch-cpu 2.14，走代理 7897）：
  - _sanity_check_gen.py：atk 桩跑三 generator 全量 profile（8880/96/30），校验维度、cu 单调/段长≤8、chunk 对齐与重放一致、HV%H、K/V、BSND B*T=total_tokens、yaml 快照与 case0 一致。全过。
  - _sanity_check_exec.py：torch 真实执行三 executor run_cpu（6/96/30 spec），校验 shape/finiteness/dh0 无状态恒 0/state 形状/dA 存储块**严格上三角非零、下三角含对角恒零**（test_da.py golden 本身以 b_dA.T 存储，非 bug）。全过。
  - 修过 3 个真 bug：BSND total_tokens 语义（B*T 用满容量）、speculative max_step 越界、num_accepted 缺省 key；校验脚本两次断言方向写反（cc 4 元组、da 三角方向）已纠正。
- _stub_common/ 是校验用 torch 版 _ascendc_common_executor 桩；common/ 是被 _sanity_check_exec 早先误写的自引用桩（后被覆盖为真桩内容），两者仅服务校验脚本，交付源仓时不需要。
